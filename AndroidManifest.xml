<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
      package="net.miller"
      android:versionCode="1"
      android:versionName="1.0">
    <application android:icon="@drawable/icon" android:label="@string/application_name">
        <activity android:name=".Gesturer"
                  android:label="@string/application_name"
                  android:theme="@android:style/Theme.NoTitleBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
	    <activity
            android:name="CreateGestureActivity"
            android:label="@string/label_create_gesture" />
        <activity
            android:name="GestureBuilderActivity"
            android:label="@string/application_name"
            android:icon="@drawable/ic_gesturebuilder" />
        <activity android:name="net.miller.GesturesActivity" />
    </application>
    <uses-sdk android:minSdkVersion="11" android:targetSdkVersion="11"/>

<uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"></uses-permission>
</manifest> 